// TODO: streaming chat UI. Backend endpoint should stream tokens (SSE) per AGENTS.md —
// every answer rendered here MUST display its source citation(s).
export default function ChatWindow() {
  return <div>{/* streaming chat UI with citations */}</div>;
}
